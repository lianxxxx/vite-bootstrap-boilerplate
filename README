# Frontend Mentor Bootstrap Manage Landing Page

## Getting Started 🚀

This project uses **Vite** and **Bootstrap**. All the packages you need are already listed in `package.json`.

### Step 1: Install Everything

All you need to do is run:

```bash
npm install
```

> ✅ That’s it! This is the **only command** you need to get all the dependencies ready.

### Step 2: Start the Development Server

Once the installation is done, start your project with:

```bash
npm run dev
```

Your project should now be running locally, and you’re ready to go! 🌟

---


